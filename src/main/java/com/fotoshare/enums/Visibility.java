package com.fotoshare.enums;

/**
 * Enum representing the visibility level of a photo.
 */
public enum Visibility {
    /**
     * Photo is only visible to the owner and users with explicit share permissions.
     */
    PRIVATE,
    
    /**
     * Photo is visible to everyone.
     */
    PUBLIC
}